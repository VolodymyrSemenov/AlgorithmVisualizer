from AlgorithmVisualizer import AlgorithmVisualizer


if __name__ == '__main__':
    interface = AlgorithmVisualizer(49, 15) # (X, Y)
    # Both have to be odd for random maze gen, X has to be 49 for text to scale
